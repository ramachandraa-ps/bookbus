specVersion: "v1"
serviceId: "bookbus"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "bookbus-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
